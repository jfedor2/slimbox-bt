/ {
    chosen {
        zephyr,console = &uart21;
        zephyr,shell-uart = &uart21;
        nordic,rpc-uart = &uart21;
    };

    aliases {
        status-led = &led0;
    };
};

&uart21 {
    current-speed = <921600>;
};
